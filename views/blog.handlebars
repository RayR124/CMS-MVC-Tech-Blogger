
<div class="row mt-4">
    <div class="col-md-6">
        <h3>Create a New Blog:</h3>

        <form class="newBlog">
            <div>
                <input class="form" type="text" id="blogTitle" name="blogTitle" placeholder="Title of your Blog" />
            </div>

            <div class="form">
                <textarea class="description" id="blogDescription" name="blogDescription"
                    placeholder="What do you have to say?"></textarea>
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-primary">Make it happen, Cap'n!</button>
            </div>
        </form>
    </div>

    <script src="/js/newBlog.js"></script>